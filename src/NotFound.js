import React from 'react';
import { Box } from '@material-ui/core';

export const NotFound = () => {
    return (
        <Box>
            Not Found
        </Box>
    );
}

export default NotFound;
